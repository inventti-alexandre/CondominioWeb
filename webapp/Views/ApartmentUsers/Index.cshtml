@model IEnumerable<BuildingProject.Model.ApartmentUser>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.apartment.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.user.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.apartment.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.user.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.apartmentUserID }) |
            @Html.ActionLink("Details", "Details", new { id=item.apartmentUserID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.apartmentUserID })
        </td>
    </tr>
}

</table>
